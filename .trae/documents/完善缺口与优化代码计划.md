## 覆盖性检查
- 已实现：鉴权（跟踪 username）、长连与并发会话、任务分发（30s 更新 server_nonce、递增 job_id、每会话维护最新 nonce、历史 job 记录）、提交校验（job_id/nonce 校验、SHA256 比对、1秒限速、重复 nonce 检测）、统计（按分钟聚合，默认 memory 存储）、消息处理器（内存队列，异步统计）。
- 可选加分项：PostgreSQL 与 RabbitMQ 作为可插拔后端暂未实现具体驱动，当前通过接口与 README 说明支持替换；如需可继续补充。

## 优化项
1. 修复 `ChannelImpl.SetReadWait` 赋值错误（当前写入 `writeWait`）。
2. 鉴权超时：`Acceptor.Accept` 设置 `ReadDeadline(timeout)`，防止卡死。
3. 广播可停止：`Coordinator` 增加停止通道或 `context`，`AppServer.Shutdown` 触发停止，防止测试挂起与泄露。
4. 线程安全：广播时会话更新改为持锁更新，避免数据竞争；清理过期 `UsedNonces` 减少内存增长。
5. 任务过期（加分）：实现 `expireAfter` 检查，过期返回 `Task expired`。
6. 客户端频率控制：
   - 明确 1 次/秒上限与至少 1 次/分钟保底的双定时器策略，避免边界不提交。
   - 处理提交响应，错误日志输出，便于调试。
7. 单元测试自动终止：
   - 为所有启动的服务端增加 `Shutdown` 调用，等待 worker 结束；
   - 将广播周期缩短并加 `context` 取消，避免 goroutine 泄露；
   - 去除阻塞读取（如 `net.Pipe` 读响应），仅断言状态与返回值；
   - 使用 `t.Cleanup` 统一清理资源。
8. README 扩展：完整构建运行、协议、测试、扩展点说明与示例。

## 实施顺序
1) 修复 `SetReadWait` 与 `Accept` 超时；2) 为广播引入停止机制并在 `Shutdown` 中清理；3) 优化 Listener 过期检查与 nonce 清理；4) 改造客户端双定时器；5) 调整与补充测试以自动结束；6) 扩展 README。

## 交付与验证
- 运行 `go build ./...` 与 `go test ./... -v` 全量通过；
- 实测端到端流程正常、测试不再卡死，支持干净退出。

确认后我将按以上步骤进行代码优化与测试完善。